{
  "name": "reservation",
  "module": "server/index.ts",
  "type": "module",
  "scripts": {
    "app:build": "bun install --ci && cd ./frontend && bun install --production && bun run build",
    "start": "bun server/index.ts",
    "dev": "bun --watch server/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.2",
    "hono": "^4.6.17",
    "zod": "^3.24.1"
  }
}